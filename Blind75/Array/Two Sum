class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        hash_map = {}
        for i, num in enumerate(nums):
            tmp = target - num
            if tmp in hash_map:
                return [hash_map[tmp], i]
            hash_map[num] = i
        
